def line(line)
    if line.length == 0
      puts "The line is currently empty."
    else
     line.each_with_index { |name, i|" #{i + 1} #{name}"}
        puts "The line is currently".join()
    end 
  end 

def take_a_number
  
end